mutation Register($input: UserInput!) {
  register(input: $input) {
    errors {
      field
      message
    }
    user {
      id
      username
      firstName
      lastName
      email
      createdAt
      updatedAt
    }
  }
}
