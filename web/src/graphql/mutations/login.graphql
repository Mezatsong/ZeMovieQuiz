mutation Login($input: LoginInput!) {
  login(input: $input) {
    user {
      id
      username
      firstName
      lastName
      email
      createdAt
      updatedAt
    }
    accessToken
  }
}
